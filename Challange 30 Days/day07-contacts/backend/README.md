# Smart Contacts API

نظام خلفي (Backend System) متكامل لإدارة جهات الاتصال، مصمم لمحاكاة تطبيقات الهاتف الحديثة. يركز المشروع على معالجة البيانات بكفاءة عالية وتوفير تجربة مستخدم متقدمة من خلال ميزات البحث الشامل وإدارة الوسائط.

## وصف المشروع

هذا المشروع هو **الجزء السابع** من سلسلة التحدي البرمجي. يهدف النظام إلى حل مشاكل الأداء عند التعامل مع قوائم البيانات الطويلة باستخدام تقنية **تقسيم الصفحات (Pagination)**. كما يوفر بنية تحتية مرنة تدعم رفع الصور الشخصية (Avatars)، وفصل الرموز الدولية عن أرقام الهواتف، بالإضافة إلى محرك بحث ذكي يعتمد على Model Scopes للبحث في حقول متعددة بآن واحد.

## المميزات التقنية

| الميزة                 | الوصف                                                                                            |
| ---------------------- | ------------------------------------------------------------------------------------------------ |
| **نظام تقسيم الصفحات** | تم ضبط النظام لعرض 10 سجلات لكل صفحة لضمان سرعة الاستجابة وتقليل الحمل على الخادم                |
| **إدارة الوسائط**      | دعم كامل لرفع الصور وتخزينها، مع خاصية توليد رابط الوصول المباشر `photo_url` بشكل تلقائي         |
| **نطاقات البحث**       | استخدام `scopeFilter` لتنفيذ عمليات بحث معقدة تشمل الاسم، رقم الهاتف، والتفاصيل الإضافية         |
| **هيكلة البيانات**     | فصل حقل رمز الدولة `country_code` عن رقم الهاتف `phone` لتسهيل عمليات الفلترة والتصنيف الدولي    |
| **الحقول المحسوبة**    | توفير حقول افتراضية مثل `full_phone` و `photo_url` لتسهيل عملية ربط البيانات مع الواجهة الأمامية |

## توثيق الواجهة البرمجية (API Endpoints)

| البروتوكول | المسار (Route)       | الوصف           | ملاحظات تقنية                                           |
| ---------- | -------------------- | --------------- | ------------------------------------------------------- |
| `GET`      | `/api/contacts`      | عرض القائمة     | يدعم معاملات `page` للتنقل و `search` للبحث             |
| `POST`     | `/api/contacts`      | إضافة جهة اتصال | يتطلب `multipart/form-data` لرفع الصور                  |
| `GET`      | `/api/contacts/{id}` | عرض التفاصيل    | يعرض بيانات السجل المحدد                                |
| `POST`     | `/api/contacts/{id}` | تعديل البيانات  | يجب إرسال `_method=PUT` ضمن البيانات للتعامل مع الملفات |
| `DELETE`   | `/api/contacts/{id}` | حذف السجل       | حذف نهائي لجهة الاتصال                                  |

## هيكل البيانات للإضافة (Request Payload)

عند إرسال طلب إضافة (`POST`)، يجب أن تكون البيانات بصيغة **Form-Data** كالتالي:

| الحقل          | النوع | متطلب | الوصف                             |
| -------------- | ----- | ----- | --------------------------------- |
| `first_name`   | نص    | ✅    | الاسم الأول                       |
| `last_name`    | نص    | ❌    | الاسم الأخير                      |
| `country_code` | نص    | ✅    | رمز الدولة (مثال: `+966`)         |
| `phone`        | نص    | ✅    | رقم الهاتف                        |
| `photo`        | ملف   | ❌    | صورة جهة الاتصال (jpeg, png, jpg) |
| `details`      | نص    | ❌    | ملاحظات إضافية                    |

## التثبيت والإعداد

لإعداد المشروع وتشغيله على البيئة المحلية:

### 1. تثبيت حزم الاعتماد

```bash
composer install
```

### 2. إعداد قاعدة البيانات والبيانات الأولية

```bash
php artisan migrate --seed
```

### 3. إنشاء اختصار التخزين

> خطوة ضرورية لتتمكن من الوصول للصور المرفوعة عبر المتصفح.

```bash
php artisan storage:link
```

### 4. تشغيل الخادم المحلي

```bash
php artisan serve
```

---

**المشروع جزء من سلسلة: تحدي 30 يوم برمجة** 🚀
